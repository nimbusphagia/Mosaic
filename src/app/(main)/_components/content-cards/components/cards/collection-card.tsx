import { CollectionLazy } from "@/_types/collections"

type CollectionCardProps = {
  collection: CollectionLazy,
  createdAt: Date;
}
export default function CollectionCard({ collection, createdAt }: CollectionCardProps) {
  return (
    <div>

    </div>
  )
}
